# 飞书转 Markdown 工具接口文档

## 基础信息

- 服务前缀：`/api`
- 统一成功响应：

```json
{
	"success": true,
	"requestId": "string",
	"data": {}
}
```

- 统一失败响应：

```json
{
	"success": false,
	"requestId": "string",
	"code": "string",
	"message": "string",
	"timestamp": "2026-03-28T10:00:00.000Z"
}
```

- 请求头：
	- `Content-Type: application/json`
	- 可选：`x-request-id`（不传会自动生成并回传）

---

## 1. 创建运行时配置

### 接口

- Method：`POST`
- Path：`/api/configs`

### 说明

用于创建一套飞书和 OBS 的运行时配置，返回 `configId` 供转换接口使用。

### 请求体

```json
{
	"name": "prod-config",
	"feishu": {
		"appId": "cli_xxx",
		"appSecret": "xxx"
	},
	"storage": {
		"endpoint": "https://obs.cn-east-3.myhuaweicloud.com",
		"region": "cn-east-3",
		"bucket": "my-bucket",
		"publicBaseUrl": "https://cdn.example.com",
		"accessKeyId": "AKIAxxx",
		"secretAccessKey": "xxx"
	}
}
```

### 字段约束

- `name`：可选，字符串
- `feishu.appId`：必填，字符串
- `feishu.appSecret`：必填，字符串
- `storage.endpoint`：必填，字符串
- `storage.region`：必填，字符串
- `storage.bucket`：必填，字符串
- `storage.publicBaseUrl`：必填，字符串
- `storage.accessKeyId`：必填，字符串
- `storage.secretAccessKey`：必填，字符串

### 成功响应示例

```json
{
	"success": true,
	"requestId": "d0c7c4b7-4b7d-4ce4-b4fe-f08cbf4d9a33",
	"data": {
		"configId": "2e2b7c9f-4f0e-4f52-b2ff-2e5174a0870e",
		"name": "prod-config"
	}
}
```

### 可能错误

- `400` 参数校验失败（缺字段或类型错误）
- `500` 内部错误（例如配置存储异常）

---

## 2. 飞书文档转换为 Markdown

### 接口

- Method：`POST`
- Path：`/api/convert`

### 说明

输入飞书文档 URL，返回解析后的 Markdown。若文档中有图片，会下载并上传到 OBS，再将 Markdown 中图片链接替换为公网 URL。

### 请求体（推荐：使用 configId）

```json
{
	"docUrl": "https://xxx.feishu.cn/docx/ABCDEF123456",
	"configId": "2e2b7c9f-4f0e-4f52-b2ff-2e5174a0870e"
}
```

### 请求体（调试：inlineConfig）

仅当环境变量 `ENABLE_DEBUG_INLINE_CONFIG=true` 时可用。

```json
{
	"docUrl": "https://xxx.feishu.cn/wiki/ABCDEF123456",
	"inlineConfig": {
		"feishu": {
			"appId": "cli_xxx",
			"appSecret": "xxx"
		},
		"storage": {
			"endpoint": "https://obs.cn-east-3.myhuaweicloud.com",
			"region": "cn-east-3",
			"bucket": "my-bucket",
			"publicBaseUrl": "https://cdn.example.com",
			"accessKeyId": "AKIAxxx",
			"secretAccessKey": "xxx"
		}
	}
}
```

### 字段约束

- `docUrl`：必填，字符串，必须匹配飞书文档 URL（`/docx/{token}` 或 `/wiki/{token}`）
- `configId`：可选，字符串
- `inlineConfig`：可选，对象（仅调试模式开启时允许）
- `configId` 与 `inlineConfig` 至少可用一种，否则报错 `MISSING_CONFIG`

### 成功响应示例

```json
{
	"success": true,
	"requestId": "a91f9e33-0554-44ef-9de5-c95cd5d77e1f",
	"data": {
		"docToken": "ABCDEF123456",
		"title": "文档标题",
		"markdown": "# 标题\n\n正文...",
		"imageCount": 2,
		"images": [
			{
				"token": "boxcnxxx",
				"key": "2e2b7c9f-4f0e-4f52-b2ff-2e5174a0870e/ABCDEF123456/2b4f...e3.png",
				"url": "https://cdn.example.com/2e2b7c9f-4f0e-4f52-b2ff-2e5174a0870e/ABCDEF123456/2b4f...e3.png",
				"success": true
			}
		]
	}
}
```

### 常见错误码

- `MISSING_CONFIG`（400）：未提供 `configId`，且未启用或未提供 `inlineConfig`
- `INVALID_DOCUMENT_URL`（400）：`docUrl` 格式不合法
- `CONFIG_NOT_FOUND`（404）：`configId` 不存在
- `UNSUPPORTED_DOC_TYPE`（400）：wiki 指向的对象不是 docx
- `FEISHU_AUTH_FAILED`（401）：飞书鉴权失败
- `FEISHU_AUTH_REQUEST_FAILED`（401/502）：请求飞书鉴权接口失败
- `FEISHU_API_ERROR`（401/502）：飞书 API 返回业务错误
- `FEISHU_REQUEST_FAILED`（502）：调用飞书 API 失败
- `FEISHU_MEDIA_DOWNLOAD_FAILED`（401/502）：下载飞书图片失败
- `OBS_UPLOAD_FAILED`（502）：上传 OBS 失败
- `IMAGE_UPLOAD_PARTIAL_FAILED`（502）：部分图片上传失败

---

## 附：调用顺序建议

1. 先调用 `POST /api/configs` 保存配置并拿到 `configId`
2. 再调用 `POST /api/convert`，传入 `docUrl + configId`
