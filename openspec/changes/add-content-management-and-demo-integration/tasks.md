## 1. 数据模型与基础设施

- [x] 1.1 选定数据库与接入方式（Supabase/Postgres），在项目中补充环境变量与连接配置
- [x] 1.2 创建 `articles`、`demos`、`article_demos` 表结构与必要索引（含 `slug` 唯一约束）
- [ ] 1.3 编写数据库初始化与迁移脚本，并在本地/预发环境验证可重复执行

## 2. 内容访问层实现

- [x] 2.1 在前端新增内容仓储接口（`listArticles`、`getArticleBySlug`、`listDemos`、`listDemosByArticle`）
- [x] 2.2 实现数据库读取适配器并输出统一的前端数据结构
- [x] 2.3 增加静态数据兜底适配器与数据源切换开关（feature flag / env）

## 3. 页面接入改造

- [x] 3.1 将 Blog 页面改造为通过内容仓储层读取文章列表
- [x] 3.2 将 Post 页面改造为通过 `slug` 读取文章详情并处理 not-found
- [ ] 3.3 将 Lab 页面改造为通过内容仓储层读取 demo 列表及文章关联 demo

## 4. 内容更新流程

- [ ] 4.1 定义标准内容提交格式（Markdown + Frontmatter）及字段校验规则
- [ ] 4.2 实现内容校验脚本（schema、必填字段、链接检查）并输出可读错误信息
- [ ] 4.3 实现内容同步脚本，将通过校验的内容幂等写入数据库

## 5. 迁移与发布保障

- [ ] 5.1 将现有 `src/app/data/posts.ts` 与 demo 数据导出并导入数据库
- [ ] 5.2 建立发布前验证流程：校验通过且同步成功后才允许发布
- [ ] 5.3 制定回滚策略：数据库异常时切回静态兜底并验证页面可用性

## 6. 验收与文档

- [ ] 6.1 为关键场景补充测试用例（重复 `slug`、草稿不可见、demo 关联顺序）
- [ ] 6.2 更新项目文档，说明内容更新流程、数据模型与常见故障处理
- [ ] 6.3 在预发环境完成端到端验收并记录上线检查清单
