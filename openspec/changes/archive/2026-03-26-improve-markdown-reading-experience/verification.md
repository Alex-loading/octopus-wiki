## 验收记录（2026-03-26）

### 自动化验证

- 执行 `npm run build`，构建通过。
- 结果：项目可成功打包，无 TypeScript/构建阻断错误。

### 手动验收清单

- 文章详情页可正常渲染基础 Markdown 元素（标题、段落、列表、引用、链接、行内代码、代码块、分隔线）。
- GFM 元素可正常渲染（表格、任务列表、删除线）。
- 目录可与标题锚点联动，点击目录项可跳转到对应小节。
- 明暗主题切换后，正文层级、间距与对比度保持一致。
- 移动端与桌面端均可阅读代码块（含横向滚动）。

### 回退方案

如线上出现兼容或样式异常，按以下顺序回退：

1. 在 `src/app/pages/Post.tsx` 恢复旧的手写 `MarkdownRenderer` 实现。
2. 保留数据层不变，仅回退渲染层逻辑，避免影响文章读取链路。
3. 重新执行 `npm run build` 验证回退分支可发布。
4. 回退后记录异常 Markdown 样本，后续在独立分支修复并二次发布。
