## 1. 依赖与渲染管线接入

- [x] 1.1 在前端项目中新增 `react-markdown`、`remark-gfm`、`rehype-sanitize`、`rehype-slug`、`rehype-autolink-headings` 依赖
- [x] 1.2 在文章详情页中建立标准化 Markdown 渲染组件并替换现有逐行解析逻辑

## 2. Markdown 元素映射与样式统一

- [x] 2.1 为核心 Markdown 元素（标题、段落、列表、引用、链接、行内代码、代码块、分隔线）建立统一组件映射
- [x] 2.2 按现有站点主题完成明暗模式下的阅读样式（层级、间距、对比度）对齐
- [x] 2.3 接入标题锚点自动生成与可点击跳转，确保与目录导航联动一致

## 3. 安全与兼容验证

- [x] 3.1 配置并验证 markdown 渲染的 sanitize 白名单策略，拦截不安全内容
- [x] 3.2 使用真实文章样本回归验证 GFM 语法（表格、任务列表、删除线）渲染正确性
- [x] 3.3 完成手动验收（浅色/深色、移动端/桌面端）并记录回退方案
