## 1. 数据访问层能力

- [x] 1.1 在 `src/app/content/repository.ts` 新增文章精选状态更新方法（输入文章 ID 与目标 featured 值，输出统一 WriteResult）。
- [x] 1.2 复用现有管理员身份校验与错误映射逻辑，确保未登录/非管理员返回明确错误信息。
- [x] 1.3 为更新方法补充最小返回字段（至少含 `id` 与 `featured`）并验证失败分支可恢复。

## 2. 管理后台交互

- [x] 2.1 在 `src/app/pages/AdminArticles.tsx` 的文章行中增加“设为精选/取消精选”按钮。
- [x] 2.2 接入 loading/禁用态，避免同一行操作期间重复提交。
- [x] 2.3 操作成功后更新当前行 featured 状态并展示成功反馈；失败时展示错误反馈并恢复按钮可操作。

## 3. 联调与验收

- [x] 3.1 使用管理员账号验证“设为精选”与“取消精选”两条链路均可成功落库。
- [x] 3.2 使用非管理员或未登录状态验证写入被拒绝且提示信息正确。
- [x] 3.3 验证首页精选区域对 `featured` 状态变更的展示结果符合预期。
